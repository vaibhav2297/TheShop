# Create Product — SDD Status

**Feature:** `create-product`
**Last updated:** 2026-09-06 (E2E re-run: AC-30/AC-34 now pass cleanly — prior timeouts confirmed transient; AC-19/AC-26 product defects still present, unchanged)

| Stage | State | Gate | Evidence | Date |
|---|---|---|---|---|
| 1. Spec       | Confirmed | ✅ spec-gate pass | appendix empty · 27 FRs · 20 RULEs · 38 ACs · amended 2026-08-14: product-level SKU (shared namespace, required at every save, suggested) + product names now unique (RULE-2 flipped, AC-22 rewritten) | 2026-08-14 |
| 2. Plan       | Resolved | ✅ plan-gate pass | 10 items resolved · 1 risk accepted · 38/38 ACs mapped · 35 tasks · variants inline (Figma 2687:14713/15441) · realigned to the amended SKU/name rules | 2026-08-14 |
| 3. Implement  | Done  | ✅ scope + build gates pass | 4 layers built 2026-08-14 · **2026-09-05 reopen:** local `supabase/migrations/` was missing the 4 migrations (`create_product_variants`, `products_admin_writes`, `save_product_rpc`, `create_product_variants_lint_fixes`) applied earlier via MCP directly to the remote `TheShop-Dev` project — a fresh local `supabase db reset` failed on `product_variants does not exist`. Reconciled by pulling the exact applied SQL from the remote project's `schema_migrations.statements` and committing it locally as `0022`-`0025`, renumbering `0022_remove_product_stock.sql`→`0026` (`0027` unchanged); local schema cross-checked column-for-column and policy-for-policy against remote. Infra scope gate clean (after fixing a stale allow-list in `check-sdd-gates.ps1` that never included `supabase/migrations/`), full solution build 0 errors, `TheShop.Infrastructure.Tests` 222/222 pass against the reconciled local stack | 2026-09-05 |
| 4. Test       | Failing | 🔴 AC coverage | 251/251 reconciled · 251/251 tests pass · 33/37 ACs ✅ — AC-26/31/36/37 Not Covered (AC-26 blocked by a real stock-tracking gap in the merged implementation; AC-31/36/37 are out of unit-test scope by design) — see [test-report.md](./test-report.md) | 2026-09-05 |
| 5. Verify     | Pending | 🔴 2 e2e AC failed (AC-19, AC-26) + unit ACs uncorroborated (Test row Failing) | 20/22 e2e ACs ✅ · 13 unit · 2 manual — AC-19/AC-26 are confirmed product defects (Price wrongly `Required` on a draft; stock quantity entirely unimplemented, unchanged from 2026-09-05); AC-30/AC-34 (previously unresolved after 3 repair rounds) now pass cleanly on a cold-started environment, confirming the prior timeouts were transient resource contention, not a code defect — see [e2e-report.md](./e2e-report.md) | 2026-09-06 |
| 6. Review     | —     | — | — | — |
| 7. Document   | —     | — | — | — |

**Shipped:** 2026-09-06 → `dev` (via PR) — ⚠️ waived: shipped with 4 open ledger item(s)

**Next step:** `/theshop.e2e` re-ran to completion on 2026-09-06 and is still NOT VERIFIED (see [e2e-report.md](./e2e-report.md)), but the blocker list is now shorter. AC-30/AC-34's edit-path timeouts did **not** reproduce on a freshly cold-started environment — no `UpdateProductHandler`/`ProductForm.SaveAsync` investigation needed. Two things still block VERIFIED: (1) re-add stock-quantity tracking (Domain `Product`/`ProductVariant`, `CreateProductCommand`/`UpdateProductCommand` + validators, `stock_quantity` wiring through `save_product`, `ProductForm`/`ProductVariantsCard` stock fields — FR-8, RULE-6, RULE-14, RULE-16; `0026_remove_product_stock.sql` deliberately dropped the columns, so they need to be re-added at the schema level too) — this also fixes AC-26; (2) fix `ProductForm.razor`'s product-price `ShopMoneyField`, which is hardcoded `Required="true"` and blocks AC-19's Unpublished-draft-with-no-price save (RULE-15). Then re-run `/theshop.test-merged create-product` (adds AC-26 coverage, flips the Test row to Passing) and `/theshop.e2e create-product` again.
